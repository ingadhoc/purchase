<?xml version="1.0"?>
<odoo>
    <record id="view_move_form_inherit_purchase" model="ir.ui.view">
        <field name="name">account.move.supplier.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="purchase.view_move_form_inherit_purchase"/>
        <field name="arch" type="xml">
            <field name="invoice_line_ids" position="before">
                <button name="update_prices_with_supplier_cost" string="Update Supplier Prices" type="object" confirm="By accepting they will update (or create prices) for this provider and all products loaded on the invoice. Are you sure you wish to continue?" invisible="move_type not in ['in_invoice', 'in_refund']"/>
            </field>


            <!-- La columna de OC depende de parent.move_type y, cuando la condición no se
                 cumple, la opción desaparece del menú de columnas opcionales en lugar de
                 quedar destildada, así que el usuario no tiene forma de recuperarla.
                 La dejamos siempre disponible como columna opcional (oculta por defecto). -->
            <xpath expr="//field[@name='invoice_line_ids']/list/field[@name='purchase_order_id']" position="attributes">
                <attribute name="column_invisible"/>
            </xpath>

            <div name='button_box' position="inside">
                <field name="has_purchases" invisible="1"/>
            </div>
        </field>
    </record>

</odoo>
